import { TypeScale } from "ask-ah-mah";

// The named type scale — display, heading, emphasis, dense, micro, eyebrow.
// Each row samples the live `var(--text-*)` size against its intended use.
export const Scale = () => <TypeScale />;
