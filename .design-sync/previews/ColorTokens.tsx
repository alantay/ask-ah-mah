import { ColorTokens } from "ask-ah-mah";

// The full palette — every semantic color role as a live swatch, grouped by
// purpose (Brand, Surface, Line & ink, Semantic, Sidebar). Swatches read their
// values from CSS variables, so they always reflect the active theme.
export const Palette = () => <ColorTokens />;
