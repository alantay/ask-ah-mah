import { Typefaces } from "ask-ah-mah";

// The three typefaces — Fraunces (display/recipe voice), Inter (body/UI), and
// Nunito (wordmark only). Each samples its live `var(--font-*)` family.
export const Faces = () => <Typefaces />;
